# 📸 New Member Photos

Drop photos here with **lowercase English name + id** matching the `id` field in `data.json`.

**Format:** `images/temp/<lowercase-id>.jpg` (3:4 portrait, ideally 900x1200px+)

After confirming the photo + info, we'll move it from `temp/` to `images/` and add the entry to `data.json`.